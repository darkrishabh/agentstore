interface:
  display_name: "AgentStore Routing"
  short_description: "Save and retrieve portable agent-owned objects"
  default_prompt: "Use $agentstore-routing to save or retrieve my information."
dependencies:
  tools:
    - type: "mcp"
      value: "agentstore"
      description: "Connected AgentStore MCP server with put, get, list, search, and delete tools."
policy:
  allow_implicit_invocation: true
